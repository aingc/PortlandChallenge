# PortlandChallenge
My solution uses Node.js so make sure you have it installed. Found here: [Node.js](https://nodejs.org/en/download/)

Run the solution in the command line with: `node deduplicateJSONLeads.js [JSON_FILE_IN_SAME_DIRECTORY]`

e.g. `node deduplicateJSONLeads.js leads.json`